import { FiltroBitacoraPipe } from './filtro-bitacora.pipe';

describe('FiltroBitacoraPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroBitacoraPipe();
    expect(pipe).toBeTruthy();
  });
});
